{
    "version": "0.2.0",
    "configurations": [
        {   
            //folder in which is runn app
            "cwd": "${workspaceFolder}\\src",
            "name": "(msvc) Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "externalConsole": true,
            "program": "${workspaceFolder}\\build\\subprojects\\Build\\MilionersCPP_core\\Debug\\MyApp.exe",
            "preLaunchTask": "Build with CMake"
        }
    ]
}